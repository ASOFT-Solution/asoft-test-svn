@(Html.Kendo().ComboBoxFor(m=>m).Name("Product").DataTextField("IDProduct").DataValueField("IDProduct")
    .Filter("startswith")
.DataSource(ds =>
{
    ds.Read(read =>
    {
        read.Action("getProductID", "TestMaster");
    }).ServerFiltering(false);
}).Suggest(true)
        .Events(e => { e.Select("SelectValueCombo");  })
)
