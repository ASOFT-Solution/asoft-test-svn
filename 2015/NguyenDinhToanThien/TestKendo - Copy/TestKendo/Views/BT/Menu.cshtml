@model List<MyConnection.MenuDaCap>

@{/* @helper ListArrayItems(int m_id) {
 foreach(var i in Model)
 {
     if (m_id == i.MaParent)
    {
    <li>
        @i.Ten
        <ul>
           @ListArrayItems(i.MaMenu)
        </ul>
    </li> 
    }
 }
} 

<ul id="mn1">
@{foreach(var i in Model)
    {
        if (i.MaParent != null)
        {
            continue; 
        }
    <li>
        @Html.Raw(i.Ten)
        <ul>
           @ListArrayItems(i.MaMenu)
        </ul>
    </li>
    }
}
</ul> */}

@(Html.Kendo().Menu()
      .Name("Menu")
      .Items(items =>
      {
          items.Add()
              .Text("Thông tin điều hành");
          items.Add()
              .Text("POS")
              .Items(children =>
              {
                  children.Add().Text("Thiết lập");
                  children.Add().Text("Danh mục")
                      .Items(inner => {
                          inner.Add().Text("Danh mục cửa hàng");
                          inner.Add().Text("Danh mục hàng hóa");
                          inner.Add().Text("Danh mục hội viên");
                          inner.Add().Text("Danh mục hình thức thanh toán");
                      });
                  children.Add().Text("Nghiệp vụ");
                  children.Add().Text("Báo cáo");             
              });
          items.Add()
              .Text("CRM").Enabled(false);
          items.Add()
              .Text("QLHV");
      })
)

<style>
    #menu h2 {
        font-size: 1em;
        text-transform: uppercase;
        padding: 5px 10px;
    }
    #template img {
        margin: 5px 20px 0 0;
        float: left;
    }
    #template {
        width: 380px;
    }
    #template ol {
        float: left;
        margin: 0;
        padding: 10px 10px 0 10px;
    }
    #template:after {
        content: ".";
        display: block;
        height: 0;
        clear: both;
        visibility: hidden;
    }
    #template .k-button {
        float: left;
        clear: left;
        margin: 5px 0 5px 12px;
    }

</style>


